{
    "name": "mattwyskiel-dotcom",
    "main": "index.ts",
    "private": true,
    "scripts": {
        "typecheck": "tsc --noEmit",
        "typecheck:watch": "tsc --noEmit --watch",
        "deploy": "pulumi up --stack prod -y",
        "deploy:dev": "pulumi up --stack dev -y"
    },
    "devDependencies": {
        "@types/bun": "latest"
    },
    "peerDependencies": {
        "typescript": "catalog:"
    },
    "dependencies": {
        "@pulumi/pulumi": "catalog:infra",
        "@whiskey/pulumi-utils": "workspace:*"
    }
}
